name: crystal-secp256k1-zkp
version: 0.1.0

authors:
  - syalon

crystal: 1.1.1

scripts:
  # postinstall: git clone https://github.com/syalon/secp256k1-zkp.git && cd secp256k1-zkp && echo $(pwd) && ./autogen.sh && ./configure && make
  postinstall: cd secp256k1-zkp && echo $(pwd) && ./autogen.sh && ./configure && make

license: MIT
