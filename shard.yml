name: crystal-secp256k1-zkp
version: 0.1.0

authors:
  - syalon

crystal: 1.1.1

scripts:
  postinstall: echo $(pwd) && git submodule update --init --recursive && cd secp256k1-zkp && ./autogen.sh && ./configure && make

license: MIT
